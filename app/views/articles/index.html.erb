<ul>
  <% @articles.each do |article| %>
    <li class="article-title"> <%= link_to article.title, article_path(article), class: "link" %>   </li>
    <li class="article-content"><%= article.content %></li>
  <% end %>
</ul>

